[build]
  builder = "nixpacks"
  buildCommand = "pnpm install && pnpm --filter backend db:migrate:deploy && pnpm --filter backend db:generate"

[deploy]
  startCommand = "pnpm start"
  healthcheckPath = "/api/health"
  healthcheckTimeout = 30
  restartPolicyType = "on_failure"